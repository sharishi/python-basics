# 📘 **Лекция: Создание и использование веб-эндпоинтов**


# Глава 1. Введение в API и веб-приложения

## 1.1. Что такое API?

**API (Application Programming Interface)** — это интерфейс взаимодействия между программами.

* Представьте: у вас есть сервер, который хранит данные, и клиент (браузер, мобильное приложение), который хочет эти данные получить.
* API описывает **правила общения** между ними: какие запросы можно делать, что ожидать в ответ.

Например:

* интернет-магазин: API возвращает список товаров, добавляет заказ, изменяет статус заказа.
* мессенджер: API получает список сообщений, отправляет новое сообщение.

**Идея:** вместо того чтобы клиент и сервер напрямую «дёргали» друг друга через файлы, API даёт **структурированное, предсказуемое и безопасное взаимодействие**.

## 1.2. HTTP-запросы: основа API

API чаще всего использует **HTTP-протокол**. Основные методы:

| Метод  | Назначение                             |
| ------ | -------------------------------------- |
| GET    | Получить данные                        |
| POST   | Создать новые данные                   |
| PUT    | Полностью обновить существующие данные |
| PATCH  | Частично обновить существующие данные  |
| DELETE | Удалить данные                         |

**Пример:**

* GET `/users` → получить список пользователей
* POST `/users` → создать нового пользователя
* GET `/users/5` → получить пользователя с ID = 5

HTTP-запросы передают данные через:

* URL-параметры (`/users/5`)
* Query-параметры (`/users?age=25`)
* Тело запроса (обычно JSON для POST/PUT)

# Глава 2. Что такое FastAPI

FastAPI — это **Python-фреймворк для создания веб-приложений и API**.

### Основные особенности:

1. **Быстрота** — работает на асинхронном сервере Uvicorn, поддерживает async/await.
2. **Простота** — эндпоинты описываются как обычные функции.
3. **Валидация данных** — через Python типы и Pydantic.
4. **Автодокументация** — Swagger UI и ReDoc создаются автоматически.
5. **Современные возможности** — поддержка WebSockets, Dependency Injection, Background Tasks.

**Идея:** вместо ручного разбора HTTP-запросов, FastAPI делает это автоматически, проверяет данные, возвращает JSON и генерирует документацию.

# Глава 3. Что такое эндпоинт

**Эндпоинт** — это URL, по которому клиент взаимодействует с сервером.

Пример:

| URL         | Метод  | Описание                     |
| ----------- | ------ | ---------------------------- |
| /           | GET    | Главная страница             |
| /users      | GET    | Получить всех пользователей  |
| /users      | POST   | Создать нового пользователя  |
| /users/{id} | GET    | Получить пользователя по ID  |
| /users/{id} | PUT    | Обновить данные пользователя |
| /users/{id} | DELETE | Удалить пользователя по ID   |

**Идея:** каждый эндпоинт — это точка входа в приложение, «порт» для запросов и ответов.


# Глава 4. Установка FastAPI и Uvicorn

Для работы с FastAPI необходимо установить:

```bash
pip install fastapi uvicorn
```

* **FastAPI** — сам фреймворк
* **Uvicorn** — ASGI-сервер, который запускает приложение и обрабатывает HTTP-запросы

После установки можно создавать полноценные веб-приложения и API на Python.

# Глава 5. Минимальное приложение с одним эндпоинтом

Создадим файл `main.py`:

```python
from fastapi import FastAPI

app = FastAPI()  # создаём экземпляр приложения

@app.get("/")
def read_root():
    return {"message": "Hello, FastAPI!"}
```

### Пояснения:

* `app = FastAPI()` — создаёт приложение, которое будет обрабатывать запросы
* `@app.get("/")` — декоратор, который говорит: «для GET-запроса на URL `/` вызови эту функцию»
* `read_root()` — функция-эндпоинт, возвращает словарь, который FastAPI автоматически конвертирует в JSON

### Запуск:

```bash
uvicorn main:app --reload
```

* `main` — имя файла без `.py`
* `app` — объект FastAPI
* `--reload` — сервер автоматически перезагрузится при изменении кода

Открой браузер [http://127.0.0.1:8000](http://127.0.0.1:8000)
Результат:

```json
{"message": "Hello, FastAPI!"}
```

# Глава 6. Эндпоинты с параметрами

## 6.1. Параметры пути

Эндпоинты могут принимать переменные прямо из URL:

```python
@app.get("/users/{user_id}")
def get_user(user_id: int):
    return {"user_id": user_id, "name": f"User {user_id}"}
```

* `user_id` из URL `/users/5` попадёт в функцию
* FastAPI автоматически конвертирует строку в `int`
* Если передать не число — FastAPI вернёт ошибку 422

Запрос: `GET /users/5`
Ответ:

```json
{"user_id": 5, "name": "User 5"}
```

## 6.2. Query-параметры

Query-параметры передаются через `?` в URL:

```python
@app.get("/items/")
def get_items(skip: int = 0, limit: int = 10):
    return {"skip": skip, "limit": limit}
```

Запрос: `/items/?skip=5&limit=20`
Ответ:

```json
{"skip": 5, "limit": 20}
```

**Идея:** query-параметры удобны для фильтров, пагинации и поиска.


# Глава 7. POST-запросы и валидация данных

Для POST-запросов данные передаются в **теле запроса** (обычно JSON).

FastAPI использует **Pydantic** для валидации и типизации данных.

```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class User(BaseModel):
    name: str
    age: int

@app.post("/users/")
def create_user(user: User):
    return {"message": f"User {user.name} created!", "user": user}
```

Пример тела запроса:

```json
{
  "name": "Ivan",
  "age": 25
}
```

Ответ сервера:

```json
{
  "message": "User Ivan created!",
  "user": {"name": "Ivan", "age": 25}
}
```

### Пояснение:

* `User` — модель данных, описывает, какие поля должны прийти
* FastAPI автоматически проверяет типы (`str`, `int`) и наличие обязательных полей
* Если данные неверные → FastAPI вернёт ошибку с пояснением


# Глава 8. Автоматическая документация

FastAPI создаёт **Swagger UI** и **ReDoc** автоматически:

* Swagger: [http://127.0.0.1:8000/docs](http://127.0.0.1:8000/docs)
* ReDoc: [http://127.0.0.1:8000/redoc](http://127.0.0.1:8000/redoc)

Можно тестировать эндпоинты прямо в браузере, отправлять GET/POST запросы и смотреть ответы.


# Глава 9. Выводы и ключевые идеи

1. **Эндпоинт** — это точка входа в серверное приложение.
2. **FastAPI** позволяет быстро создавать эндпоинты с минимальным кодом.
3. **GET и POST** — основные HTTP-методы, но поддерживаются и PUT, PATCH, DELETE.
4. **Параметры пути, query-параметры и JSON-тела запроса** дают гибкость при работе с данными.
5. **Автодокументация** — мощный инструмент для тестирования и взаимодействия с API.
6. Создание веб-приложения через FastAPI — это по сути **создание API**, которое может использовать фронтенд, мобильные приложения или другой сервер.

